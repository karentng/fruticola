{% extends "base.html.twig" %}

{% block breadcrumnb %}
    <ul class="breadcrumb">
      <li class="active"><i class="icon16 i-home-4"></i>Inicio</li>
    </ul>
{% endblock %}

{% block titulo %} <h1><i class="icon20 i-man"></i> Listado de RUATS Inscritos </h1> {% endblock %}

{% block contenido %}

<a class="btn btn-lg btn-primary" href="{{site_url("ruata/index")}}">Crear Nuevo RUAT</a>
<br/><br/>

<table class="table table-bordered table-hover">
    <thead>
        <th>#</th>
        <th>Productor</th>
        <th>Identificación</th>
        <th>Creado</th>
        <th></th>
    </thead>
    <tbody>
        {% for row in ruats %}
        {% set theid = row.__get('id') %}
        <tr>
            <td>{{row.id}}</td>
            <td>{{row.productor.nombre_completo()}}</td>
            <td>{{row.productor.tipo_documento.descripcion}}: {{row.productor.numero_documento}}</td>
            <td>{{row.creado | date("Y-m-d  H:i")}}</td>
            <td><a class="act act-warning" href="{{site_url(['ruata','index',row.id])}}">Editar</a></td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}
